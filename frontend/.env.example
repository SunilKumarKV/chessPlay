# Frontend public environment placeholders only.
# Do not place backend secrets, database URLs, JWT secrets, payment secrets, or private API keys here.

VITE_API_URL=http://localhost:3001
VITE_BACKEND_URL=http://localhost:3001
VITE_SOCKET_URL=http://localhost:3001

# Public client-side keys only. Secret keys must stay in backend/hosting provider env settings.
VITE_RAZORPAY_KEY_ID=
VITE_ANALYTICS_PROVIDER=
VITE_ANALYTICS_KEY=
VITE_SENTRY_DSN=
